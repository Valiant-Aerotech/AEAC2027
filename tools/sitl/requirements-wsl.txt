# ArduPilot SITL build deps - install inside WSL Ubuntu (not Windows .venv).
#
# Preferred (Ubuntu 24.04+, avoids PEP 668 pip restrictions):
#   sudo apt install -y python3-empy
#
# Alternative:
#   python3 -m pip install --user --break-system-packages empy==3.3.4
empy==3.3.4
