# Core - required for all missions
numpy>=1.24
opencv-python>=4.8
pymavlink>=2.4
PyYAML>=6.0

# Task 2 GCS offload (scrcpy screen capture)
mss>=9.0
pygetwindow>=0.0.9

pyserial>=3.5                 # COM serial for pymavlink (GCS / FC)

# Computer vision models (YOLO dry detector — ONNX uses onnxruntime, .pt uses ultralytics)
onnxruntime>=1.16
ultralytics>=8.0

# Google Drive upload (optional - only needed for gdrive_service_account)
# google-api-python-client>=2.100
# google-auth>=2.23
